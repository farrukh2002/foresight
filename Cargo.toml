[package]
name = "foresight"
version = "0.5.0-beta"
edition = "2024"

[dependencies]
inotify = { version = "0.11.5", default-features = false }
serde = { version = "1.0.229", features = ["derive"] }
toml = "1.1.4"

[features]
bench = []

[profile.release]
opt-level = 3
lto = true
strip = true
